import HelpButton from './HelpButton'
import { delivery } from 'images'

const DeliveryHelpButton = () => (
    <HelpButton icon={delivery} label={'Free home delivery when you spend over £50'} />
)

export default DeliveryHelpButton